# CLI-001 — Tasks

| ID | Title | Covers | Description |
|---|---|---|---|
| T001 | Extend `ToolMeta` with `instruction` type and `destination` field | R001, R002 | In `packages/cli/src/shared/interfaces/IToolsRepository.ts`, add `"instruction"` to the `type` union and add the optional field `destination?: string` to the `ToolMeta` interface |
| T002 | Add `destination` to `InstallOptions` | R008, R009 | In `packages/cli/src/shared/interfaces/ITargetAdapter.ts`, add the optional field `destination?: string` to the `InstallOptions` interface |
| T003 | Validate `destination` presence for `instruction` type in `FsToolsRepository` | NF002 | In `FsToolsRepository.getContent`, before resolving content, check if `meta.type === "instruction"` and `!meta.destination`; if so, throw `Error: Component "<name>" of type "instruction" is missing the required "destination" field in meta.json.` |
| T004 | Accept `dest` override in `AddUseCase.execute` | R004, R012 | In `AddUseCase.execute`, add an optional third parameter `dest?: string`; assign `options.destination = dest` when building the `InstallOptions` object passed to `adapter.install` |
| T005 | Add `instruction` branch to `ClaudeTarget.install` | R008, R010 | In `ClaudeTarget.install`, add an `else if (toolType === "instruction")` branch that calls a new private method `writeInstruction(absoluteDestination: string, content: string)` which performs `mkdir(dirname(dest), { recursive: true })` and `writeFile(dest, content, "utf8")`; throw if `options.destination` is absent |
| T006 | Add `instruction` branch to `CopilotTarget.install` | R009, R010 | In `CopilotTarget.install`, add an `else if (toolType === "instruction")` branch that performs `mkdir(dirname(dest), { recursive: true })` and `writeFile(dest, content, "utf8")`; throw if `options.destination` is absent |
| T007 | Add `--dest` flag and destination resolution to `handler.ts` | R004, R012 | In `registerAddCommand`, add `.option("--dest <path>", "Override install destination path")` to the command definition; after retrieving `toolMeta`, compute `resolvedDest = options.dest ?? toolMeta.destination` and store it for use in subsequent steps |
| T008 | Implement overwrite confirmation in `handler.ts` for `instruction` type | R005, R006, R007, NF001 | In `registerAddCommand`, after resolving `resolvedDest`, when `toolMeta.type === "instruction"` and `resolvedDest` is defined: compute `absoluteDest = join(process.cwd(), resolvedDest)` and, if `existsSync(absoluteDest)`, call Clack `confirm({ message: \`"${resolvedDest}" already exists. Overwrite?\` })`; if the result is not `true`, call `log.warn("Installation cancelled.")` and `process.exit(0)`; otherwise proceed to call `useCase.execute(toolName, targetName, resolvedDest)` |
| T009 | Create `ds-CLAUDE` component `meta.json` | R011 | Create `tools/duck-spec/instructions/ds-CLAUDE/meta.json` with fields `name: "ds-CLAUDE"`, `type: "instruction"`, `description`, `tags`, `targets: ["claude"]`, and `destination: "CLAUDE.md"` |
| T010 | Create `ds-CLAUDE` fallback `instructions.md` | R011, R003 | Create `tools/duck-spec/instructions/ds-CLAUDE/instructions.md` with the generic duck-spec CLAUDE.md instructions content (used as fallback when no target-specific variant exists) |
| T011 | Create `ds-CLAUDE` Claude variant `variants/claude.md` | R011, R003, R008 | Create `tools/duck-spec/instructions/ds-CLAUDE/variants/claude.md` with Claude Code–specific CLAUDE.md instructions content for the duck-spec workflow |
| T012 | Add `ds-CLAUDE` to the `duck-spec` kit | R011 | In `tools/duck-spec/meta.json`, append `"ds-CLAUDE"` to the `components` array |
