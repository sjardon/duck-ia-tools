# CLI-002 — Tasks

| ID | Title | File | Symbol | Covers | Description |
|---|---|---|---|---|---|
| T001 | Add `AdditionalFile` interface and `files` field to `ToolMeta` | `packages/cli/src/shared/interfaces/IToolsRepository.ts` | `AdditionalFile`, `ToolMeta` | R001, R002 | Export a new `AdditionalFile` interface with `fileName: string` and `content: string`; add `files?: string[]` to `ToolMeta` |
| T002 | Add `getAdditionalFiles` to `IToolsRepository` | `packages/cli/src/shared/interfaces/IToolsRepository.ts` | `IToolsRepository` | R002, R003 | Add `getAdditionalFiles(name: string): Promise<AdditionalFile[]>` to the `IToolsRepository` interface |
| T003 | Add `additionalFiles` to `InstallOptions` | `packages/cli/src/shared/interfaces/ITargetAdapter.ts` | `InstallOptions` | R003 | Import `AdditionalFile` from `IToolsRepository` and add `additionalFiles: AdditionalFile[]` to `InstallOptions` |
| T004 | Implement `getAdditionalFiles` in `FsToolsRepository` | `packages/cli/src/shared/repositories/FsToolsRepository.ts` | `FsToolsRepository.getAdditionalFiles` | R002, R006, NF001 | Implement `getAdditionalFiles(name)`: resolve the component directory, iterate over `meta.files ?? []`, read each file with `readFile`, and throw `Error: Component "<name>" declares additional file "<relPath>" which does not exist at "<absolutePath>".` when the file is missing (ENOENT); return `AdditionalFile[]` with `fileName = path.basename(entry)` |
| T005 | Call `getAdditionalFiles` in `AddUseCase.execute` | `packages/cli/src/commands/add/add.useCase.ts` | `AddUseCase.execute` | R003 | After `getContent`, call `this.repository.getAdditionalFiles(toolName)` and include the result as `additionalFiles` in the `InstallOptions` object passed to `adapter.install` |
| T006 | Add `writeAdditionalFiles` helper to `ClaudeTarget` | `packages/cli/src/infrastructure/targets/ClaudeTarget.ts` | `ClaudeTarget.writeAdditionalFiles` | R004 | Implement private `writeAdditionalFiles(destDir: string, additionalFiles: AdditionalFile[]): Promise<void>` that calls `mkdir(destDir, { recursive: true })` then `writeFile(join(destDir, file.fileName), file.content, "utf8")` for each entry |
| T007 | Call `writeAdditionalFiles` from `installAgent` | `packages/cli/src/infrastructure/targets/ClaudeTarget.ts` | `ClaudeTarget.installAgent` | R004 | After writing the main agent `.md` file, call `this.writeAdditionalFiles(agentDir, options.additionalFiles)` |
| T008 | Call `writeAdditionalFiles` from `installSkill` | `packages/cli/src/infrastructure/targets/ClaudeTarget.ts` | `ClaudeTarget.installSkill` | R004 | After writing `SKILL.md`, call `this.writeAdditionalFiles(skillDir, options.additionalFiles)` |
| T009 | Call `writeAdditionalFiles` from `writeInstruction` | `packages/cli/src/infrastructure/targets/ClaudeTarget.ts` | `ClaudeTarget.writeInstruction` | R004 | After writing the instruction file, call `this.writeAdditionalFiles(dirname(absoluteDestination), options.additionalFiles)` |
| T010 | Call `writeAdditionalFiles` from hook and mcp-server branches | `packages/cli/src/infrastructure/targets/ClaudeTarget.ts` | `ClaudeTarget.install` | R004 | In the `hook` and `mcp-server` branches of `install`, after the respective `updateSettings*` call, call `this.writeAdditionalFiles(join(projectPath, ".claude"), options.additionalFiles)` |
| T011 | Update `ds-analysis` meta.json with `"files"` field | `tools/duck-spec/skills/ds-analysis/meta.json` | `files` | R005 | Add `"files": ["analysis.template.md"]` to the JSON object |
| T012 | Update `ds-design` meta.json with `"files"` field | `tools/duck-spec/skills/ds-design/meta.json` | `files` | R005 | Add `"files": ["design.template.md", "tasks.template.md"]` to the JSON object |
